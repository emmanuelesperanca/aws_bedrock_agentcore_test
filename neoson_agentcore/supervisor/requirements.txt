# Neoson Supervisor — Python dependencies
# Pin major versions for reproducibility

# AWS Bedrock AgentCore SDK
bedrock-agentcore>=0.1.0

# Strands agent framework (ReAct + BedrockModel)
strands-agents>=0.1.0

# A2A protocol client (sub-agent delegation — Fase 4)
a2a-sdk>=0.1.0

# AWS SDK
boto3>=1.38.0
botocore>=1.38.0

# Utilities
httpx>=0.27.0
